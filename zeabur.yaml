# Zeabur Configuration
name: kasir-api

# Build settings
build:
  commands:
    - go mod tidy
    - go build -o kasir-api .

# Runtime settings
run:
  command: ./kasir-api

# Port configuration
port: 8080

# Environment
env:
  PORT: 8080